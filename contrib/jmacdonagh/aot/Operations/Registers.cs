using System;

namespace SharpOS.AOT
{
	/// <summary>
	/// The registers that are available for use.
	/// </summary>
	public enum Registers
	{
		AL,
		AH,
		AX,
		EAX,
		BL,
		BH,
		EBX,
		CL,
		CH,
		CX,
		ECX,
		DL,
		DH,
		DX,
		EDX,
		EBP
	}
}
